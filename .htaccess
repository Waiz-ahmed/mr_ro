<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve build assets directly from public_html/build/
    RewriteRule ^build/(.*)$ build/$1 [L]

    # Everything else goes to public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>